{
  "name": "register",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/utils/register/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["utils®"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/packages/utils/register"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
